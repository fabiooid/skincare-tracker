{
  "name": "atelier",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Starts the API on port 4111 and the web app on port 5173. The paid formulator agent needs GEMINI_API_KEY from Cursor Cloud settings or .env (OPENAI_API_KEY is the fallback). Optional GEMINI_MODEL defaults to google/gemini-2.5-flash."
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 4111 }
  ]
}
